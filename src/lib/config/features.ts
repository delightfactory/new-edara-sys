export const VISITS_ATOMIC_EXECUTION = import.meta.env.VITE_VISITS_ATOMIC_EXECUTION === 'true';

// Preview branch override: Sidebar V2 is intentionally enabled here so the
// Design System can be visually reviewed without changing production/main.
export const DESIGN_SYSTEM_V2_SIDEBAR = true;
