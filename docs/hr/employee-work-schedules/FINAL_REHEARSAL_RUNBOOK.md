# Employee Work Schedules — Disposable Rehearsal Runbook

> **وثيقة تنفيذ البروفة الحالية، وليست إذنًا بالتطبيق أو التفعيل.**
>
> المرجع التفصيلي لأسماء الملفات وترتيبها هو `CURRENT_MIGRATION_MANIFEST.md`.

## 1. حدود التنفيذ

- الهدف قاعدة Supabase/PostgreSQL محلية Disposable من نفس مخطط الإنتاج الحالي.
- الإنتاج قراءة فقط.
- لا Supabase Branch مدفوع، ولا Preview أو Build على Vercel.
- الخاصية والجاهزية تظلان `false` طوال تركيب السلسلة والفحوص البنيوية.
- لا جداول لموظفين حقيقيين ولا Backfill للحضور أو الإجازات.
- لا إعادة حساب لمسير راتب قائم.
- أي اختلاف في مخطط الإنتاج أو Hashes الدوال يوقف البروفة.
- تُراجع كل دفعة منفصلة قبل الانتقال للدفعة التالية.

## 2. Baseline قبل التطبيق

احفظ قبل M1:

- Migration history.
- تعريفات وACLs دوال الحضور والجزاءات والإجازات والرواتب.
- تعريفات Triggers الإجازات، وخاصة `trg_leave_submission` و`trg_leave_approval`.
- قيم وأشكال إعدادات أوقات الشركة، بما فيها القيمة الفعلية `1100` إن ظلت موجودة.
- ACL/RLS/Triggers جدول `company_settings`.
- وظائف cron الخاصة بالحضور.
- أعداد صفوف الموظفين والحضور والسجلات والجزاءات والإجازات والأرصدة والرواتب والتنبيهات.
- أعداد مسيرات `approved` و`paid` وروابط القيود المحاسبية.
- عدد طلبات الإجازة المعتمدة وعدد أيام الحضور المرتبطة بها.

## 3. تطبيق الميجريشنات

طبّق الملفات الـ40 بالترتيب الحرفي الموجود في `CURRENT_MIGRATION_MANIFEST.md`.

بعد كل مرحلة شغّل Verification المقابل قبل الانتقال للمرحلة التالية. الفشل يوقف السلسلة، ولا يتم تخطي الملف أو تعديل قاعدة الاختبار يدويًا لإجباره على النجاح.

### دفعة الإجازات

بعد `20260805215000` طبّق بالترتيب:

1. `20260806180000_hr_employee_work_schedules_leave_integration.sql`
2. `20260806181000_hr_employee_work_schedules_leave_balance_settlement.sql`
3. `20260806181500_hr_employee_work_schedules_leave_cross_year_compatibility.sql`
4. `20260806181700_hr_employee_work_schedules_partial_unpaid_leave.sql`
5. شغّل `20260806182000_hr_employee_work_schedules_leave_integration_verify.sql`

### دفعة إعدادات الشركة

بعد نجاح الإجازات طبّق بالترتيب:

1. `20260806190000_hr_employee_work_schedules_company_history.sql`
2. `20260806190500_hr_employee_work_schedules_company_history_resolution_fix.sql`
3. `20260806190700_hr_employee_work_schedules_company_history_hardening.sql`
4. `20260806190800_hr_employee_work_schedules_company_history_guard_correction.sql`
5. `20260806191000_hr_settings_atomic_update.sql`
6. `20260806191200_hr_settings_company_baseline_sync.sql`
7. `20260806191300_hr_settings_company_consistency_constraint.sql`
8. `20260806191400_hr_company_work_schedule_read_policy.sql`
9. `20260806191500_hr_settings_current_company_version_alignment.sql`
10. `20260806191600_hr_settings_atomic_concurrency_guard.sql`
11. `20260806191700_hr_settings_alignment_lock_order.sql`
12. `20260806191800_hr_settings_internal_key_guard.sql`
13. شغّل `20260806192000_hr_employee_work_schedules_company_history_verify.sql`
14. شغّل `20260806192100_hr_settings_current_company_version_alignment_verify.sql`
15. شغّل `20260806192200_hr_settings_atomic_concurrency_verify.sql`
16. شغّل `20260806192300_hr_settings_alignment_lock_order_verify.sql`
17. شغّل `20260806192400_hr_settings_internal_key_guard_verify.sql`

لا تُركب واجهة سجل الشركة في صفحة الإعدادات أثناء هذه الخطوة. المكوّن موجود للمراجعة الساكنة فقط حتى تنجح البروفات وفحوص TypeScript.

## 4. المحاكاة

افتح جلسة Disposable واحدة:

```sql
SET SESSION edara.allow_schedule_simulation = 'disposable-only';
```

ثم شغّل بالترتيب:

1. `20260805200700_hr_employee_work_schedules_final_lifecycle_simulation.sql`
2. `20260805200600_hr_employee_work_schedules_company_and_transition_duration_simulation.sql`
3. `20260805201000_hr_employee_work_schedules_runtime_payroll_simulation.sql`
4. `20260805214500_hr_employee_work_schedules_offday_payroll_simulation.sql`
5. `20260805215200_hr_employee_work_schedules_permission_snapshot_simulation.sql`
6. `20260805215300_hr_employee_work_schedules_zero_day_partial_payroll_simulation.sql`
7. `20260806183000_hr_employee_work_schedules_leave_lifecycle_simulation.sql`
8. `20260806184000_hr_employee_work_schedules_unpaid_leave_payroll_simulation.sql`
9. `20260806185000_hr_employee_work_schedules_partial_unpaid_leave_simulation.sql`
10. `20260806193000_hr_employee_work_schedules_company_history_simulation.sql`
11. `20260806193100_hr_settings_company_baseline_sync_simulation.sql`
12. `20260806193200_hr_settings_current_company_version_alignment_simulation.sql`

كل ملف يجب أن يعلن النجاح قبل `ROLLBACK`، وبعده يجب ألا تبقى أي بيانات محاكاة أو تغيير في تعريف Feature Helper.

### اختبار التزامن متعدد الجلسات

بعد نجاح الملفات السابقة، افتح جلستين مستقلتين على قاعدة Disposable:

1. الجلسة الأولى تبدأ معاملة وتستدعي `update_hr_settings_atomic` ثم تحتفظ بالمعاملة مفتوحة مؤقتًا.
2. الجلسة الثانية تستدعي RPC نفسها بقيم مختلفة وتجب أن تنتظر قفل `hr_settings_atomic`.
3. بعد إنهاء الأولى، يجب أن تعيد الثانية قراءة الحالة بعد القفل، لا أن تطبق Bundle مشتقة من قيم قديمة.
4. كرر السيناريو بين `update_hr_settings_atomic` و`align_legacy_company_settings_to_current_version` لإثبات عدم وجود Deadlock وترتيب القفل HR ثم سجل الشركة.
5. تُلغى المعاملات ويُثبت تطابق العدادات مع Baseline.

## 5. سيناريوهات الجداول والحضور الملزمة

- أحمد نعمة الله: 6 ساعات يوميًا مع فترتي 15–21 و10–16.
- المبيعات: 9 ساعات يوميًا.
- اختلاف وقت البداية والنهاية مع ثبات مدة اليوم.
- رفض مدة مختلطة داخل نفس النسخة.
- رفض تغيير المدة منتصف الشهر وقبولها يوم 1.
- التأخير بعد فترة السماح نفسها في GPS والتعديل الإداري.
- الإضافي يبدأ بعد نهاية الموظف، ولا ينتج من Auto Checkout.
- الانصراف المبكر يستخدم دقائق التصريح المغطاة فعلًا.
- التصاريح المتداخلة لا تُحسب مرتين في الجزاء المالي.
- حضور يوم الإجازة الأسبوعية لا ينتج جزاء أو إضافيًا ولا يعوض غياب يوم عمل.
- فترة استحقاق بلا يوم عمل مقرر تعطي صفر يوم وصفر راتب، لا يومًا وهميًا.
- Snapshot المثبتة لا يمكن تغييرها.
- أي سجل غير مقرر مفتوح يظل مانع مراجعة واضحًا قبل الراتب.
- السلف والعمولات والتعديلات والتأمينات والضرائب والترحيلات لم تتغير.

## 6. سيناريوهات الإجازات الملزمة

- `days_count` المرسل من الواجهة لا يكون مرجعًا ماليًا؛ الخادم يعيد حسابه.
- الإجازة التي تمر بيوم إجازة أسبوعية أو عطلة رسمية تخصم أيام العمل فقط.
- طلب بخمسة أيام تقويمية وثلاثة أيام عمل يحجز ويستخدم ثلاثة أيام فقط.
- الاعتماد عند مستوى المشرف لا ينشئ أيام حضور قبل الاعتماد النهائي عند وجود مستويين.
- الاعتماد النهائي يُحفظ أولًا ثم تُنشأ أيام `on_leave` عبر Trigger `AFTER UPDATE`.
- كل يوم إجازة مدفوع منشأ يحتوي Snapshot عمل كاملة وصحيحة.
- إلغاء إجازة غير مرتبطة بمسير معتمد يحذف الأيام الآلية ويعيد الرصيد كاملًا.
- الإلغاء يُرفض عند وجود مسير معتمد/مدفوع أو حضور فعلي تمت تسويته.
- الحضور الكامل في يوم إجازة معتمد يعيد `used_days` ويحدث `remaining_days` مرة واحدة.
- الإجازة بدون أجر لا تنشئ جزاءً تلقائيًا، ولا تدخل كحضور مدفوع في الراتب.
- العمل لنصف يوم أثناء إجازة بدون أجر ينتج `day_value = 0.50` وخصم نصف يوم وصفر جزاءات.
- العمل الجزئي أثناء إجازة مدفوعة يظل يومًا مدفوعًا كاملًا وفق سياسة V1.
- طلب الإجازة العابر لنهاية السنة يظل مقبولًا وفق سياسة النظام الحالية ويستخدم رصيد سنة البداية.
- فشل إنشاء أي Snapshot أو تعارض يوم حضور يجعل معاملة الاعتماد كلها تتراجع، بما فيها الرصيد والحالة.

## 7. سيناريوهات إعدادات الشركة الملزمة

- استيراد Baseline واحدة من إعدادات الشركة دون تغيير صفوف `company_settings` وقت الميجريشن.
- قبول `1100` كتمثيل قديم وإرجاعه `11:00` في العقود الجديدة.
- تطابق وقت البداية والنهاية مع عدد الساعات دون افتراض ثابت `480` دقيقة.
- رفض تغيير مدة يوم الشركة في منتصف الشهر، وقبوله في اليوم الأول.
- قبول تحريك نافذة الوقت مستقبلًا مع ثبات مدة اليوم.
- استخدام النسخة التي تغطي التاريخ المطلوب، لا آخر قيمة حالية فقط.
- استمرار عطلة الموظف الأسبوعية الخاصة فوق عطلة الشركة عند عدم وجود جدول فردي كامل.
- نجاح أول جدول موظف منتصف الشهر إذا طابق مدة الشركة المؤرخة لذلك التاريخ.
- رفض أول جدول موظف مخالف للمدة منتصف الشهر.
- رفض تعديل نسخة شركة إذا كان سيبطل انتقال جدول موظف سبق تجهيزه.
- رفض تعديل نسخة شركة تتقاطع مع Snapshot شركة مثبتة، حتى لو ظلت المدة مساوية.
- التحديث الذري غير المتسق يتراجع بالكامل ولا يغير مفتاحًا واحدًا.
- التحديث الفوري قبل وجود أي تجهيز يغير الإعدادات والـBaseline معًا.
- بعد بدء التجهيز المؤرخ، يُرفض التغيير الدلالي في الحقول القديمة ويستخدم مسار النسخة المستقبلية.
- تحديث مباشر لجدول `company_settings` يفشل عند `SET CONSTRAINTS ... IMMEDIATE` إذا ترك الـBaseline غير متطابق.
- إذا أصبحت نسخة مستقبلية نافذة قبل فتح الميزة، تُصالح الإعدادات القديمة مع نسخة يوم التنفيذ بواسطة RPC مخصصة، ويُسجل Audit.
- عمليتا الحفظ المتزامنتان تتسلسلان قبل قراءة الحالة؛ لا يُبنى Update على Bundle قديمة.
- المصالحة والتحديث الذري يستخدمان ترتيب قفل واحدًا ولا يدخلان Deadlock.
- مفتاح التفعيل لا يظهر في شاشة HR العامة، وترفضه الـRPC العامة حتى لو أرسله عميل مباشر.
- كل نسخة منتهية غير قابلة للتعديل، والحذف ممنوع.
- `hr_company_work_schedule_activation_consistent()` يظل تشخيصًا فقط؛ readiness تبقى `false`.
- صاحب `settings.update` يستطيع قراءة النسخ التي يسمح له النظام بإدارتها.
- لا يستخدم التطبيق المسار القديم بعد ظهور مفتاح تركيب M1، حتى عند تأخر PostgREST في رؤية الـRPC الجديدة.

## 8. مقارنة ما بعد كل محاكاة

بعد كل `ROLLBACK` يجب أن تتطابق بيانات الأعمال مع الـBaseline، باستثناء كائنات المخطط وسجل الميجريشنات:

- الموظفون.
- الحضور والـlogs والـalerts والجزاءات.
- طلبات الإجازات والأرصدة والتصاريح.
- سجل الرواتب والفترات والمسيرات والبنود والسلف والأقساط والتعديلات.
- التنبيهات و`notification_alert_state`.
- إعدادات الشركة وسجل جدول الشركة والـcron.
- المسيرات المعتمدة والمدفوعة والقيود المرتبطة بها.

أي فرق غير متوقع = فشل صريح.

## 9. وضع الفحص النهائي

لا يوجد حاليًا Final Release Preflight صالح للاستخدام.

الملف:

```text
20260805222000_hr_employee_work_schedules_final_release_preflight.sql
```

أصبح قديمًا بعد دفعات الإجازات وإعدادات الشركة، ولا يُشغّل كدليل إصدار. سيتم إنشاء Final Preflight جديد فقط بعد اكتمال دفعات التقارير ودورة حياة الموظف والتنبيهات، ثم جولة المراجعة المستقلة.

## 10. فحوص التطبيق دون نشر

بعد نجاح قاعدة البيانات:

1. TypeScript type-check.
2. اختبارات جداول العمل وتطبيع الوقت المضغوط.
3. اختبار شاشة طلب الإجازة ومعاينة `preview_employee_leave_workday_count`.
4. اختبارات خدمات جدول الشركة والمكوّن المستقل.
5. اختبار إخفاء مفتاح التفعيل ورفض إرساله من خدمة الإعدادات العامة.
6. الاختبارات الكاملة.
7. lint.
8. مراجعة فرق الفرع كاملًا.
9. تأكيد تطابق `EmployeeProfileLegacy.tsx` مع `main`.
10. تأكيد عدم إنشاء Vercel Deployment.
11. مراجعة مستقلة لقرار تركيب `CompanyWorkScheduleSettingsCard` داخل صفحة الإعدادات.

لا يتم تشغيل Vercel Build أو Preview ضمن هذه المرحلة.

## 11. ما لا تعالجه V1

- لا ورديات ليلية.
- لا Split shifts.
- لا تتبع راحة.
- لا تكليف تلقائي بالعمل في عطلة رسمية.
- لا تقسيم رصيد طلب عابر لنهاية السنة بين سنتين؛ يستمر عقد سنة البداية الحالي.
- لا تغيير Backdated لمواعيد الشركة من الواجهة العادية.
- لا حذف لسجل جدول الشركة.
- لا ضمان لتسليم Push بعد قبول طلب HTTP في الطابور؛ التنبيه Best-effort مع مراجعة تشغيلية.
- حضور يوم غير مقرر بلا انصراف يحتاج مراجعة بشرية قبل الراتب.

## 12. الإنتاج قرار منفصل

حتى نجاح كل البروفات لا يجيز:

- تطبيق الميجريشنات على الإنتاج.
- إنشاء جداول حقيقية.
- فتح Feature Flag.
- إعادة حساب حضور أو إجازة أو راتب سابق.
- تغيير إعدادات الشركة أو cron.
- تركيب المكوّن الجديد في الواجهة المنشورة.
- إنشاء Preview أو Production Deployment.

الإنتاج يحتاج Drift review جديدًا، نافذة صيانة، خطة رجوع مثبتة، موظف Pilot بتاريخ مستقبلي، وموافقة مباشرة منفصلة.
